import React from 'react'; import {View,Text,StyleSheet,useColorScheme} from 'react-native'; import AppIcon from '../appIcon'; import {getTheme} from '../../theme';
export default function EmptyState({message='No data available'}:{message?:string}){const t=getTheme(useColorScheme()==='dark');return <View style={[s.box,{backgroundColor:t.surfaceMuted}]}><AppIcon name={'Inbox' as any} size={28} color={t.textMuted}/><Text style={[s.text,{color:t.textMuted}]}>{message}</Text></View>};const s=StyleSheet.create({box:{padding:28,borderRadius:18,alignItems:'center',gap:8},text:{fontSize:13,fontWeight:'500'}});
